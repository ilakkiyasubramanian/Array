class Solution {
    public static int findElementAtIndex(int key, int[] arr) {
        // code here
        int s=0;
            if(key>=0 && key<arr.length)
            {
                 s=arr[key];
            }
            return s;
        
    }
}
