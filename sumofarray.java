class Solution {
    int sum(int arr[]) {
        int s=0;
        // code here
        for(int i=0;i<arr.length;i++)
        {
            s+=arr[i];
            
        }
        return s;
    }
}
